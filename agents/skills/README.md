# agents/skills/

Custom skills given to Hermes / OpenClaw during the build go here as `SKILL.md`
files (one folder per skill, e.g. `agents/skills/sla-recompute/SKILL.md`).

None committed yet — add real skill files here as they're created during the
sprint loop, and reference them from `agent-log.md`.
